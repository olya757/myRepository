/*
* олл аеяякюбмше сакчдйх
*/
#define _USE_MATH_DEFINES

#include <iostream>
#include <string>
#include <stack>
#include <cmath>
#include <queue>
#include <algorithm>
#include <vector>
#include <set>
#include <map>

using namespace std;
typedef long long ll;

#define vec vector
#define pb push_back
#define ins insert

int main() {
	double a, b, c, d;
	cin >> a >> b >> c;

	if (b - a = c - b)
		cout << "Yes";
	else
	if (a == 0)
		cout << "No";
	else
	{

		d = b / a;
		if (b*d == c)
			cout << "Yes";
		else
			cout << "No";
	}

		
	return 0;
}